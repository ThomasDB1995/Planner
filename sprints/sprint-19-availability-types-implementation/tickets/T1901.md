# T1901 - Availability Helperlaag En Type Model

## Status

Uitgevoerd.

## Doel

Centraliseer availability-logica en migreer bestaande unavailable-state naar typed availability zonder gedrag buiten availability te wijzigen.

## Uitgevoerd

- `AvailabilityType` toegevoegd;
- `EmployeeAvailability` aangepast naar `type: AvailabilityType`;
- `src/lib/planning/availability.ts` toegevoegd;
- availability lookup, set, clear en toggle helpers toegevoegd;
- labelhelpers toegevoegd;
- directe availability `.some(...)` lookups vervangen door helpergebruik.

## Gewijzigde Bestanden

- `src/lib/planning/availability.ts`;
- `src/components/planning/matrix.ts`;
- `src/app/page.tsx`;
- `src/components/planning/EmployeeRow.tsx`;
- `src/components/planning/WeekPlanningBoard.tsx`.

## Buiten Scope Gebleven

- `PlanningItem` wijzigingen;
- resource helper wijzigingen;
- conflictengine wijzigingen;
- persistence/backend/API;
- drag/drop;
- packages.

## Validatie

- `npm run build` geslaagd;
- oude `isUnavailable: true` statevorm verwijderd;
- bestaande 1-click unavailable flow behouden.
