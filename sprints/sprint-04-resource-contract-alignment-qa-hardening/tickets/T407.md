# T407 - PlanningForm Integration Cleanup

Status: Voorbereid

## Doel

Maak planningformulier contractueel resource-breed.

## Exacte Scope

- Props `machines` naar `resources`.
- Form state `machineId` naar `resourceId`.
- Submit payload gebruikt `resourceId`.
- Validatiemelding gebruikt resource-terminologie.

## Expliciet Buiten Scope

- Nieuwe velden.
- Form redesign.
- Persistence.
- API submit.
- Validation framework.

## Dependency Order

Na T402, T404 en T406.

## Verwachte Filewijzigingen

- `src/components/planning/PlanningForm.tsx`
- `src/app/page.tsx`

## Implementatiestappen

1. Open `PlanningForm.tsx`.
2. Hernoem props en local state.
3. Update verplichte veldcheck naar `resourceId`.
4. Update `ResourceSelector` binding.
5. Update submit object.
6. Update `page.tsx` props/imports.
7. Run build/typecheck.

## Acceptance Criteria

- Formulier gebruikt `resourceId`.
- Geen `machineId` in `PlanningForm`.
- ResourceSelector krijgt `resources`.
- Planningitem toevoegen blijft contractueel mogelijk.

## Regressierisico's

- Submit object mist resource.
- Resetstate gebruikt oude veldnaam.
- Page geeft oude propnaam door.

## Testaanpak

- Build.
- Handmatige form submit in T410.

## Rollback Considerations

- Rollback form en page samen.
- Niet half `machineId`/`resourceId` laten staan.

## Codex-risico-inschatting

Medium. Dit is de gevoeligste UI-dataflow.
