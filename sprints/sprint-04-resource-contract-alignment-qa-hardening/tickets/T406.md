# T406 - ResourceSelector Contract Cleanup

Status: Voorbereid

## Doel

Maak `ResourceSelector` props en lokale types volledig resource-breed.

## Exacte Scope

- Type `Machine` vervangen door `Resource`.
- Props blijven `resources`, `selectedResourceId`, `onSelect`.
- UI blijft compact en ongewijzigd.

## Expliciet Buiten Scope

- UI redesign.
- Modal.
- Virtualized list.
- Nieuwe search features.
- Nieuwe filters.

## Dependency Order

Na T401 en T405.

## Verwachte Filewijzigingen

- `src/components/planning/ResourceSelector.tsx`

## Implementatiestappen

1. Open `ResourceSelector.tsx`.
2. Vervang `Machine` import door `Resource`.
3. Update `ResourceSelectorProps`.
4. Update `formatCategory` signature.
5. Laat JSX en behavior gelijk.
6. Run build/typecheck.

## Acceptance Criteria

- Component gebruikt geen `Machine` type meer.
- Props zijn resource-breed.
- Zoek/filter UI blijft zichtbaar.

## Regressierisico's

- Type mismatch met `PlanningForm`.
- Onbedoelde UI labelwijzigingen.

## Testaanpak

- Build.
- Localhost render in T410.

## Rollback Considerations

- Componentwijziging terugdraaien als form-integratie niet buildbaar is.

## Codex-risico-inschatting

Laag tot medium door propketen.
